# beffectsServer

TO USE:

Install Nodejs,
Install Nodemon,
Install Yarn,
Clone repo,
cd into repo directory,
type the following:

yarn install,
yarn dev

Then open a browser and go to localhost:3000
